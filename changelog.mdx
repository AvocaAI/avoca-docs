---
title: "Changelog"
description: "Recent updates and release history for Avoca AI"
public: true
---

import { changelogData } from '/snippets/changelog-data.jsx';

Welcome to the Avoca AI changelog. Here you'll find our release history, recent updates, and completed features across all our products.

## Release History

We ship new features and improvements weekly. Our changelog documents our commitment to:

- **Continuous Delivery** - Weekly releases with new features and improvements
- **Stability First** - Thoroughly tested updates that maintain platform reliability
- **Customer-Driven** - Features built based on your feedback and requests
- **Transparent Communication** - Clear documentation of what's changed

## Recent Updates by Product

Browse the changelog for each product to see recent releases and improvements:

<CardGroup cols={2}>
  <Card title="Responder" icon="comments" href="/changelog/responder">
    Enhanced AI conversation capabilities and multi-channel support
  </Card>
  <Card title="Coach" icon="chart-line" href="/changelog/coach">
    Customer search, analytics graphs, and performance tools
  </Card>
  <Card title="Speed-to-Lead" icon="bolt" href="/changelog/speed-to-lead">
    Advanced lead prioritization and automated follow-ups
  </Card>
  <Card title="Analytics Coach" icon="brain" href="/changelog/analytics-coach">
    Predictive insights and automated coaching plans
  </Card>
</CardGroup>

## All Recent Updates

<Tabs>
  <Tab title="By Date">
    {changelogData.releases.map((release, index) => (
      <Update key={index} label={release.date}>
        <h3>{release.product}</h3>
        {release.features.map((feature, fIndex) => (
          <div key={fIndex}>
            <h4>{feature.title}</h4>
            {feature.author && <p><em>Author: {feature.author}</em></p>}
            {feature.description && <p>{feature.description}</p>}
            <ul>
              {feature.items.map((item, iIndex) => (
                <li key={iIndex}>
                  <strong>{item.label}</strong> - {item.description}
                </li>
              ))}
            </ul>
            {feature.note && <Note>{feature.note}</Note>}
          </div>
        ))}
      </Update>
    ))}
  </Tab>
  <Tab title="By Product">
    {Object.entries(
      changelogData.releases.reduce((acc, release) => {
        if (!acc[release.product]) acc[release.product] = [];
        acc[release.product].push(release);
        return acc;
      }, {})
    ).map(([product, releases]) => (
      <div key={product}>
        <h3>{product}</h3>
        {releases.map((release, rIndex) => (
          <Update key={rIndex} label={release.date}>
            {release.features.map((feature, fIndex) => (
              <div key={fIndex}>
                <h4>{feature.title}</h4>
                {feature.author && <p><em>Author: {feature.author}</em></p>}
                {feature.description && <p>{feature.description}</p>}
                <ul>
                  {feature.items.map((item, iIndex) => (
                    <li key={iIndex}>
                      <strong>{item.label}</strong> - {item.description}
                    </li>
                  ))}
                </ul>
                {feature.note && <Note>{feature.note}</Note>}
              </div>
            ))}
          </Update>
        ))}
      </div>
    ))}
  </Tab>
</Tabs>

## Stay Updated

Want to see what's coming next? Check out our [Roadmap](/roadmap) or [contact us](mailto:support@avoca.ai) with feature requests.